# Revision Gap Audit

Date: 2026-06-18

Scope: static audit only. No local build, compile, benchmark, profiling run, or experiment was executed. Runtime verification and new data collection must be run on CloudLab using `faaspe/nodes.txt` and the project virtual environment convention already used by the scripts.

Inputs inspected:

- Paper source/PDF location provided by the user: `C:\Users\41045\Projects\Paper-prototype\FaaSPE\paper.pdf` and nearby `.tex` sources.
- Decision letter/reviewer comments: `C:\Users\41045\Projects\Paper-prototype\FaaSPE\misc\TC-DecisionLetter.txt`.
- Repository experiment/runbook/orchestration code: `faaspe/docs/revision_experiments.md`, `faaspe/scripts/*.py`, `faaspe/scripts/batch/*.py`, `faaspe/scripts/runner.py`, `faaspe/scripts/param_parser.py`.
- Runtime placement/logging/profiling code: `faaspe/lib/arbiter.py`, `faaspe/lib/profiler.py`, `faaspe/lib/benchmark.py`, `faaspe/lib/invocation_log.py`, `faaspe/lib/access_meta.py`, `faaspe/platform/function.py`.
- Paper plotting scripts found outside this repository: `C:\Users\41045\Projects\Paper-prototype\FaaSPE\scripts\draw.py`, `draw_placement_accuracy.py`, `draw_placement_accuracy_refined.py`.

Notes:

- The paper source contains revision TODOs in the evaluation/design text, including incomplete sections for fallback frequency, ablation study, overhead breakdown, AST limitations, cross-shard discussion, and formatting cleanup.
- The repository has user-owned modified/untracked experiment scripts and result files. This audit treats those as current context and does not modify them.
- Existing local result files were not treated as freshly verified experimental evidence.

## Gap Table

| Paper/reviewer requirement | Where it is claimed or needed in the paper | Current repository support | Missing implementation or missing experiment | Files likely involved | Suggested minimal fix | Priority | Whether CloudLab execution is required |
|---|---|---|---|---|---|---|---|
| Report placement accuracy against an oracle placer. | Reviewer 1 asks for prediction accuracy; Reviewer 3 explicitly asks for placement accuracy against an oracle placer. Paper `evaluation.tex` has `Placement Accuracy` and claims `FaaSPE matches the oracle placement in 3/4 access-depth settings`. | `faaspe/scripts/run_list_traversal_placement_accuracy.py` runs local/remote/FaaSPE variants for list traversal depths 1/2/4/8 and writes `placement_accuracy.csv`. `faaspe/scripts/revision_experiments.py` and `summarize_revision_results.py` can compute oracle side from `CacheOnly` and `StorageOnly`. | Evidence is narrow unless the full revision runner is used: the standalone script covers only list-traversal, while reviewer asks about the placement mechanism more generally. Paper plotting script defaults to one fixed old directory rather than a current full-run summary. | `faaspe/scripts/run_list_traversal_placement_accuracy.py`; `faaspe/scripts/revision_experiments.py`; `faaspe/scripts/summarize_revision_results.py`; paper `scripts/draw_placement_accuracy_refined.py`; `evaluation.tex`. | Use the full CloudLab revision run for all configured workloads, then make paper plotting consume `$RESULT_DIR/summary/placement_accuracy.csv` or pass an explicit `--data-dir`. Keep the standalone list-traversal plot as a focused microbenchmark, not the only evidence. | P0 | Yes. Generate data with `python scripts/cloudlab_revision_full.py` on CloudLab, or run from local only as the existing CloudLab orchestrator wrapper if it invokes CloudLab nodes. |
| Ablate static analysis vs runtime monitoring/fallback. | Reviewer 3 asks to ablate static analysis vs runtime monitoring. Paper has an empty `Ablation Study` subsection. | `revision_experiments.py` defines variants `StaticOnly` (`FAASPE_PROFILER_ENABLED=0`), `NoFallback` (`FAASPE_FALLBACK_ENABLED=0`), and `FullFaaSPE`; summary writes `summary/ablation.csv`. | No paper text/figure is complete. There is no clear `RuntimeOnly`/`NoStatic` variant that disables the RPN/AST decision and relies only on profiler exploration, so static-vs-runtime attribution is incomplete. | `faaspe/scripts/revision_experiments.py`; `faaspe/scripts/summarize_revision_results.py`; `faaspe/lib/arbiter.py`; `faaspe/lib/profiler.py`; `evaluation.tex`; paper plotting scripts. | Add or document a minimal `RuntimeOnly` variant, e.g. force unknown/default placement plus profiler/fallback enabled, then summarize alongside `StaticOnly`, `NoFallback`, and `FullFaaSPE`. Plot `summary/ablation.csv`. | P0 | Yes for measurements. Code change would be needed before running the missing variant. |
| Analyze sensitivity to static-analysis features and thresholds. | Reviewer 2 asks for formulas, threshold values, and sensitivity analysis. Reviewer 3 asks for sensitivity to static-analysis features and thresholds. Paper `design.tex` adds formulas and says thresholds are profile-calibrated; `evaluation.tex` references threshold-sensitive outputs in the runbook but the paper section is not complete. | `Arbiter` supports `FAASPE_THRESHOLD_MULTIPLIER`, `FAASPE_STORAGE_DEPTH_THRESHOLD`, `FAASPE_OBJECT_SIZE_THRESHOLD`. `revision_experiments.py` sweeps threshold multipliers and `summarize_revision_results.py` writes `summary/threshold_sensitivity.csv`. `run_placement_calibration.py` can recommend a depth threshold. | Sensitivity is limited to a storage function cost multiplier and configured workloads. There is no feature ablation for AST/RPN features such as constant loop count, parameterized loop count, missing parameter, object size, storage load, or unsupported analysis fallback. | `faaspe/lib/arbiter.py`; `faaspe/scripts/revision_experiments.py`; `faaspe/scripts/run_placement_calibration.py`; `faaspe/functions/placement-calibration/handler.py`; paper plotting scripts; `evaluation.tex`. | Keep threshold multiplier sweep, then add a small synthetic feature-sensitivity workload matrix or static analyzer unit table if code is changed later. For this revision, minimally plot `threshold_sensitivity.csv` and clearly state feature sensitivity not yet experimentally covered. | P1 | Yes for threshold sensitivity measurements. Static feature table can be generated by code inspection only, but any latency/accuracy claim requires CloudLab. |
| Report fallback frequency and explain when fallback triggers. | Reviewer 1 asks for fallback frequency. Reviewer 2 asks about fallback conditions and update frequency. Paper has an empty `Fallback Frequency` subsection and revised overhead text mentions fallback exploration. | `Profiler` tracks fallback/explore/recheck state. `Benchmark.log_invocation()` logs `fallback_triggered` and `fallback_phase`. `summarize_revision_results.py` writes `fallback_frequency`, `fallback_explore_count`, and `fallback_explore_median_latency_us`. | Paper section is TODO. The current summary does not separate exploration, override fallback, and recheck frequency in the main placement summary; it also does not directly report Profiler `fallback_count` by workload/case unless using `temp.csv` fields. | `faaspe/lib/profiler.py`; `faaspe/lib/benchmark.py`; `faaspe/scripts/summarize_revision_results.py`; `faaspe/scripts/parse_invocation_log.py`; `evaluation.tex`; paper plotting scripts. | Extend the summary or plot to report `fallback_phase` counts (`explore`, `fallback`, `recheck`) per workload/case/variant. Generate from CloudLab JSONL logs produced by the full revision run. | P0 | Yes for final frequencies. Existing parser is static, but data must come from CloudLab. |
| Quantify placement-decision/logging/profiling overhead. | Reviewer 1 and Reviewer 2 ask for overhead evaluation. Paper `evaluation.tex` has `Overhead Breakdown` with `ev_overhead_breakdown.pdf`; text claims Arbiter/Profiler add tens of microseconds. | Runtime records `arbiter_decision_us`, `profiler_update_us`, `trigger_check_us`, `ast_analysis_us`; summaries write `overhead_breakdown.csv`. `run_profile_overhead_micro.py` compares disabled/metadata/async/memory logger modes. | `ast_analysis_us` and `trigger_check_us` are always zero in current runtime path because registration-time AST extraction and trigger checks are not implemented in the Python runtime. The paper plotting script computes overhead from old standalone CSV/JSONL files, not from full-run `summary/overhead_breakdown.csv`. Logging overhead is only partially covered by `memory_logger`; no file-backend logging mode is included. | `faaspe/lib/benchmark.py`; `faaspe/lib/invocation_log.py`; `faaspe/scripts/run_profile_overhead_micro.py`; `faaspe/scripts/summarize_revision_results.py`; paper `scripts/draw_placement_accuracy_refined.py`; `evaluation.tex`. | For the current revision, report only measured Arbiter/Profiler/logging overhead that the implementation really emits. Add a `file_logger` mode if claiming JSONL write overhead. Do not claim AST or trigger runtime overhead unless implemented/measured. | P0 | Yes for overhead numbers. Code change needed if adding file logger mode. |
| Implement or accurately describe AST Analyzer limitations and unsupported patterns. | Reviewer 2 asks about nested conditionals, recursion, dynamic dispatch, async calls, unknown loop bounds, external libraries, and unsupported patterns. Paper `design.tex` has TODOs for unsupported cases and cross-shard cases; `implementation.tex` claims Python AST module support and portability. | `faaspe/lib/arbiter.py` evaluates RPN expressions and has hard-coded `DEFAULT_PROFILES`; `faaspe/platform/function.py` writes a manifest from those defaults on create. Missing or invalid profiles result in `unsupported_static_analysis` and default placement. | No actual AST parser/analyzer implementation was found. The implementation writes known profiles rather than extracting AST from function source. Unsupported-pattern handling is therefore not empirically backed beyond missing/invalid RPN fallback. | `faaspe/lib/arbiter.py`; `faaspe/platform/function.py`; function handlers; paper `design.tex`; `implementation.tex`. | Minimal safe fix is textual: revise paper to say the current artifact uses registered RPN profiles derived from static inspection, and list unsupported patterns as limitations/future work. If keeping the AST claim, implement a registration-time analyzer and tests before measuring. | P0 | Code/text audit no. Any performance claim for analyzer/fallback behavior yes. |
| Support paper claim that thresholds are profile-calibrated and periodically refreshed. | Paper `design.tex` revision text says primitive latencies are profiled at startup and periodically refreshed; Profiler recalibrates when latency deviation/storage load changes. | `run_placement_calibration.py` can run a CloudLab calibration workload and print `FAASPE_STORAGE_DEPTH_THRESHOLD`. `Arbiter` reads env vars and threshold multiplier at initialization. `Profiler` can override placement after violations. | Runtime does not automatically run startup calibration, periodically refresh primitive latencies, or update Arbiter's latency constants from Profiler policy updates. `receive_policy_update()` only stores updates. | `faaspe/lib/arbiter.py`; `faaspe/lib/profiler.py`; `faaspe/scripts/run_placement_calibration.py`; `faaspe/functions/placement-calibration/handler.py`; `design.tex`. | Either narrow the paper claim to manual/offline calibration via `run_placement_calibration.py`, or implement the policy update path so Profiler updates Arbiter cost constants. | P0 | Yes to generate calibrated values; code change needed for automatic refresh. |
| Support function trigger for large object placement. | Paper `design.tex` describes function trigger embedded in GET, large-object crossover, and transparent runtime trigger. Reviewer 2 asks about trigger handling overhead. | `Arbiter` has an object-size threshold and can choose `func` when `object_size >= FAASPE_OBJECT_SIZE_THRESHOLD`. `access_meta.py` records object-size metadata after accesses. Logs include `trigger_check_us` and `trigger_used`. | No J-KV/cache trigger path was found that embeds function id in GET and converts to FUNC after observing object size. `trigger_check_us` remains a placeholder. The arbiter can only use object size if params already include it before placement. | `faaspe/lib/arbiter.py`; `faaspe/lib/benchmark.py`; `faaspe/lib/access_meta.py`; `jkv/src/cache/*`; `jkv/src/store/*`; paper `design.tex`; `evaluation.tex`. | For revision, avoid claiming implemented transparent triggers unless implemented in J-KV/cache. Minimal experiment alternative: evaluate object-size placement with explicit `object_size`/`VALUE_LEN` params and label it as parameter-informed placement. | P0 | Yes for any trigger/object-size latency data. Code change required for transparent trigger claim. |
| Demonstrate dynamic storage-load detection/adaptation. | Paper `evaluation.tex` includes `Adaption to Storage Load`; `design.tex` says Profiler sends heartbeats and detects FUNC queue load. Reviewer 2 asks about storage-load detection and threshold updates. | Workload `storage-load-trace` exists in `revision_experiments.py`; `Arbiter` accepts `storage_load_us` param and adds it to storage-side estimate. `Profiler` detects latency deviations and explores/fallbacks. | No heartbeat implementation or FUNC queue load sampler was found. Storage load appears to be injected as a workload parameter or inferred from latency deviation, not measured by periodic heartbeats. | `faaspe/lib/arbiter.py`; `faaspe/lib/profiler.py`; `faaspe/functions/storage-load-trace/handler.py`; `faaspe/scripts/revision_experiments.py`; `jkv/src/*`; `design.tex`; `evaluation.tex`. | Rephrase claim to latency-deviation fallback unless heartbeat/queue-load detection is implemented. Use `storage-load-trace` in full CloudLab run to produce adaptation evidence. | P1 | Yes for adaptation results. |
| Clarify cold starts, cache prewarming, repetitions, variance, and concurrency. | Reviewer 2 asks for methodology details and fairness. Paper `evaluation.tex` methodology says CloudLab seven d430 servers, executor latency, and baseline setup, but omits several revision details. | `revision_experiments.py` has `--repetitions`, `--warmup-operations`, `--concurrency`, `--seed`, and writes `experiment_manifest.json`. `cloudlab_revision_full.py` defaults to 5 repetitions, 3000 operations, warmup 200, concurrency 1. Runbook documents these. | Paper text does not yet incorporate the concrete manifest/warmup/repetition/concurrency details. Summaries report per-repetition rows but no variance/confidence interval table by default. | `faaspe/docs/revision_experiments.md`; `faaspe/scripts/revision_experiments.py`; `faaspe/scripts/summarize_revision_results.py`; paper `evaluation.tex`. | Add a methodology paragraph sourced from `experiment_manifest.json`. If variance is claimed, extend summary/plots to aggregate mean/std or CI across repetitions. | P1 | Yes for final manifests and variance data. |
| Ensure plotting scripts consume required revision data. | Paper includes figures for placement accuracy and overhead; runbook lists `summary/*.csv`. | Paper-side `draw_placement_accuracy_refined.py` consumes standalone `placement_accuracy.csv`, per-case `list-traversal-...csv`, and JSONL from a hard-coded default directory. Repository has `summarize_revision_results.py` for full-run `summary/*.csv`. | No repository plotting script was found that directly consumes full-run `summary/placement_accuracy.csv`, `summary/ablation.csv`, `summary/overhead_breakdown.csv`, and `summary/threshold_sensitivity.csv` for all revision figures. Paper plotting is outside repo and defaults to a specific historical result directory. | Paper `scripts/draw_placement_accuracy_refined.py`; `faaspe/scripts/summarize_revision_results.py`; `faaspe/docs/revision_experiments.md`; paper `evaluation.tex`. | Add/adjust plotting scripts to accept `--summary-dir` and consume the summary CSVs. Keep figure generation outside runtime experiments, but make data provenance explicit. | P0 | No for script changes. Yes to create fresh input data. |
| Explain anomalies where FaaSPE is worse than Cloudburst/baselines. | Reviewer 1 cites 100KB in Figure 5 and access depth 2 in Figure 6 bottom. Paper `evaluation.tex` has a TODO for anomaly discussion. | Existing scripts produce benchmark CSVs for `data-size`, `compute-emulate`, `list-traversal`, etc. Some historical results exist under `faaspe/results`. | No dedicated anomaly-analysis script/table was found. Paper text still contains TODO. | `faaspe/scripts/batch/run_data_size.py`; `faaspe/scripts/batch/run_compute_emulate.py`; `faaspe/results/*`; paper `evaluation.tex`; paper `scripts/draw.py`. | Add a short paper discussion grounded in existing/fresh CloudLab results: identify whether anomaly is bandwidth, IPC/cache overhead, compute dominance, or storage FUNC overhead. Do not add numbers unless generated. | P1 | Yes if new anomaly numbers are needed. |
| Discuss cross-shard limitation for storage-side functions. | Reviewer 2 asks for deeper discussion. Paper `implementation.tex` states FUNC requests do not access multiple shards; `design.tex` has TODO for cross-shard cases. | J-KV and function examples appear oriented around single-shard/simple key access; no multi-shard FUNC orchestration was identified in the static audit. | Missing deeper discussion and no experiment showing limitation impact. | `implementation.tex`; `design.tex`; `jkv/src/store/*`; `jkv/src/cache/*`; function handlers. | Add a limitation paragraph: current storage-side FUNC assumes single-shard access; cross-shard functions should remain compute-side or require future distributed transaction/orchestration support. | P1 | No for discussion. Yes only if adding an experiment. |
| Strengthen isolation evidence. | Reviewer 3 says isolation mechanism is not convincing. Paper `design.tex` describes warm container pools; runbook mentions `JKV_ISOLATION_MODE`. | `faaspe/scripts/run_isolation_bench.py` and `render_isolation_table.py` exist. `faaspe/docs/revision_experiments.md` says default revision experiments use `JKV_ISOLATION_MODE=none` and isolation-specific experiments can use `lightweight` or `strong`. | Current main revision run explicitly disables isolation by default. The implementation evidence for security/resource isolation is outside the main placement evaluation and needs a focused table/discussion. | `faaspe/scripts/run_isolation_bench.py`; `faaspe/scripts/render_isolation_table.py`; `jkv/src/*`; paper `design.tex`; `evaluation.tex`. | Include isolation as a scoped limitation plus a small isolation-overhead table if CloudLab isolation results are regenerated. Avoid overstating multi-tenant security guarantees. | P1 | Yes for isolation-overhead numbers. |
| Keep overclaim about up to 99% latency reduction grounded. | Reviewer 3 says the 99% claim is from specific apps and may overclaim generality. Paper `evaluation.tex` still reports 98%-99% for File-replicator/K-hop and overall claims. | Existing benchmark scripts cover data-size, real apps, list traversal, traces, etc. | Need paper-level framing rather than code support: distinguish best-case data-intensive apps from general improvement, and show adaptive-placement-specific contribution via oracle/ablation/sensitivity. | Paper `abstract.tex`; `introduction.tex`; `evaluation.tex`; `ending.tex`; revision summary outputs. | Narrow abstract/introduction claims to "up to" best-case and pair with median/typical results plus ablation/accuracy figures. | P0 | No for wording. Yes if adding aggregate numbers. |
| Add related work note for Faa$T. | Reviewer 3 asks to discuss Faa$T transparent cache. | Not a repository implementation issue. | Missing related-work paper text. | Paper `background.tex`; `ending.tex`; `references.bib`. | Add citation/discussion in related work/background if relevant. | P2 | No. |
| Fix writing/formatting/TODOs. | Reviewers note typos and citation/style issues. Paper source contains TODOs and mojibake text in inspected output, e.g. typo notes and TODOs around evaluation. | Not a repository runtime issue. | Paper has incomplete sections and TODOs that should not ship. | Paper `.tex` files under `C:\Users\41045\Projects\Paper-prototype\FaaSPE`. | Remove TODOs, fix typos, normalize citation spacing, and ensure black text/page limit. | P1 | No. |

## CloudLab Commands For Missing Data

Use the existing venv convention from the repository. From CloudLab or from the local workstation only when using the existing CloudLab orchestration scripts:

```powershell
cd C:\Users\41045\Projects\Project-FAASPE\faaspe
$env:RESULT_DIR = "results/revision-full-<date-or-tag>"
$env:PYTHON_BIN = "..\.venv\Scripts\python"
python scripts\cloudlab_revision_full.py
```

Expected output files:

- `$RESULT_DIR/experiment_manifest.json`
- `$RESULT_DIR/raw/<workload>__<case>__<variant>__rep<r>__w<w>/metadata.json`
- `$RESULT_DIR/raw/<workload>__<case>__<variant>__rep<r>__w<w>/invocations.jsonl`
- `$RESULT_DIR/raw/<workload>__<case>__<variant>__rep<r>__w<w>/temp.csv`
- `$RESULT_DIR/summary/placement_accuracy.csv`
- `$RESULT_DIR/summary/ablation.csv`
- `$RESULT_DIR/summary/overhead_breakdown.csv`
- `$RESULT_DIR/summary/threshold_sensitivity.csv`
- `$RESULT_DIR/summary/placement_counts.csv`

Focused placement-accuracy run, if keeping the current list-traversal figure:

```powershell
cd C:\Users\41045\Projects\Project-FAASPE\faaspe
$env:PYTHON_BIN = "..\.venv\Scripts\python"
python scripts\run_list_traversal_placement_accuracy.py --repetitions 3 --num-operations 1000
```

Expected output:

- `faaspe/results/placement-accuracy-list-traversal-<timestamp>/placement_accuracy.csv`
- `faaspe/results/placement-accuracy-list-traversal-<timestamp>/list-traversal-*.csv`
- `faaspe/results/placement-accuracy-list-traversal-<timestamp>/list-traversal-*.jsonl`

Focused logging/profiling overhead run:

```powershell
cd C:\Users\41045\Projects\Project-FAASPE\faaspe
$env:PYTHON_BIN = "..\.venv\Scripts\python"
python scripts\run_profile_overhead_micro.py --strategy faaspe --depth 2 --num-operations 1000
```

Expected output:

- `faaspe/results/profile-overhead-micro-<timestamp>/profile_overhead_summary.csv`

## Assumptions

- `faaspe/nodes.txt` remains the authoritative CloudLab node list and was not edited.
- The local machine is not a valid experiment node.
- The repository venv exists at `.venv`; wrappers also support `PYTHON_BIN` to point at the desired interpreter.
- Paper files live outside this repository at `C:\Users\41045\Projects\Paper-prototype\FaaSPE`; this audit did not modify them.
